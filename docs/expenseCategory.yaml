paths:
  /api/expense-category:
    get:
      tags:
        - expense-category
      summary: getAll
      responses:
        200:
          schema:
            $ref: '#/definitions/getAllExpenseCategorySuccess'
    post:
      tags:
        - expense-category
      summary: create
      produces:
        - application/json
      parameters:
        - in: body
          name: create
          description: create expense category
          required: true
          schema:
            $ref: '#/definitions/createExpenseCategory'
      responses:
        201:
          description: OK
          schema:
            $ref: '#/definitions/createExpenseCategorySuccess'

definitions:
  getAllExpenseCategorySuccess:
    type: array
    items:
      type: object
      properties:
        _id:
          type: string
        name:
          type: string
        owner:
          type: string

  createExpenseCategory:
    type: object
    properties:
      name:
        type: string

  createExpenseCategorySuccess:
    type: object
    properties:
      _id:
        type: string
      name:
        type: string
      owner:
        type: string
