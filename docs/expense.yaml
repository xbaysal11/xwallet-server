paths:
  /api/expense:
    get:
      tags:
        - expense
      summary: getAll
      responses:
        200:
          schema:
            $ref: '#/definitions/getAllExpenseSuccess'
    post:
      tags:
        - expense
      summary: create
      produces:
        - application/json
      parameters:
        - in: body
          name: create
          description: create expense
          required: true
          schema:
            $ref: '#/definitions/createExpense'
      responses:
        201:
          description: OK
          schema:
            $ref: '#/definitions/createExpenseSuccess'

definitions:
  getAllExpenseSuccess:
    type: array
    items:
      type: object
      properties:
        expense:
          type: integer
          format: int32
        _id:
          type: string
        comment:
          type: string
        categoryId:
          type: string
        categoryName:
          type: string
        resourseId:
          type: string
        resourseName:
          type: string
        owner:
          type: string
        date:
          type: string

  createExpense:
    type: object
    properties:
      expense:
        type: integer
        format: int32
      comment:
        type: string
      categoryId:
        type: string
      resourceId:
        type: string

  createExpenseSuccess:
    type: object
    properties:
      total:
        type: integer
        format: int32
      expenseModel:
        type: object
        properties:
          expense:
            type: integer
            format: int32
          _id:
            type: string
          comment:
            type: string
          categoryId:
            type: string
          categoryName:
            type: string
          resourseId:
            type: string
          resourseName:
            type: string
          owner:
            type: string
          date:
            type: string
