paths:
  /api/transfer:
    get:
      tags:
        - transfer
      summary: getAll
      responses:
        200:
          schema:
            $ref: '#/definitions/getAllTransferSuccess'
    post:
      tags:
        - transfer
      summary: create
      produces:
        - application/json
      parameters:
        - in: body
          name: create
          description: create transfer from one wallet to another
          required: true
          schema:
            $ref: '#/definitions/createTransfer'
      responses:
        201:
          description: OK
          schema:
            $ref: '#/definitions/createTransferSuccess'

definitions:
  getAllTransferSuccess:
    type: array
    items:
      type: object
      properties:
        amount:
          type: integer
          format: int32
        _id:
          type: string
        comment:
          type: string
        resourceId:
          type: string
        resourceName:
          type: string
        destinationId:
          type: string
        destinationName:
          type: string
        owner:
          type: string
        date:
          type: string

  createTransfer:
    type: object
    properties:
      amount:
        type: integer
        format: int32
      comment:
        type: string
      resourceId:
        type: string
      destinationId:
        type: string

  createTransferSuccess:
    type: object
    properties:
      total:
        type: integer
        format: int32
      transfer:
        type: object
        properties:
          amount:
            type: integer
            format: int32
          _id:
            type: string
          comment:
            type: string
          resourceId:
            type: string
          resourceName:
            type: string
          destinationId:
            type: string
          destinationName:
            type: string
          owner:
            type: string
          date:
            type: string